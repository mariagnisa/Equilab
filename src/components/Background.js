import BackgroundImage from '../assets/img/bakgrund_fade.png';

const Background = {
  backgroundImage: 'url(' + BackgroundImage  + ')',
  backgroundSize: 'cover',
  backgroundPosition: 'center',
  height: '100vh',
};

export default Background;